# Domain docs

Use a single-context layout:
- CONTEXT.md at the project root
- docs/adr/ for architecture decisions

Before exploring, read CONTEXT.md and relevant ADRs.
If absent, proceed silently. Domain-modeling creates them
when terms and decisions are resolved.

Use the glossary's vocabulary consistently.
Surface conflicts with existing ADRs instead of silently
overriding their decisions.
