# Shared Bridge sum with tanh soft-clip

Each Bridge sums up to four Slave DAWs with tanh soft-clip, replacing one-producer-per-input rejection, because four DAWs must share the same channel set and loudness must be preserved without silent averaging.
