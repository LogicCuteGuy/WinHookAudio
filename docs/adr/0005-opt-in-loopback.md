# Opt-in virtual loopback

Virtual slots get a per-slot Loop flag with Worker OUT-to-IN copy next tick, instead of no passthrough, because VRChat to VRCT monitoring needs a DAW-processed loop without an extra track while keeping default paths isolated.
