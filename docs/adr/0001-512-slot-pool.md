# 512 In + 512 Out absolute pool

v10.1 replaces the proposed 32-in/32-out bank with 512 inputs + 512 outputs absolute, dynamic 1..512, because templates need stable indices and Bridge types live inside the pool rather than as extra channels.
