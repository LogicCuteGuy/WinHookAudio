# Five-tab panel with header-only first slice

The control panel uses INPUTS/OUTPUTS/NETWORK/GENERAL/ABOUT tabs instead of a two-row grid, and the first slice is WHASlotTable.h plus SHM names plus slots.json offline only, because 512 slots need filtered tab views and ABI must stabilize before any audio claim.
