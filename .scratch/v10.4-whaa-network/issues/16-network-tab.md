# 16: NETWORK 8 tab

**What to build:** `NETWORK 8` tab `Tx1..8`/`Rx1..8` `192.168.1.50:6980` `PCM_F32`/`VORBIS` `Q0.4` `Ch32` `BW` in `WHAControlPanelUI.cpp` one `ImGui` code for Master & Bridge, `INPUTS` `Type=NETWORK` `Rx1` linkage.

**Blocked by:** 14-pcm-jitter-codebook.

**Status:** ready-for-agent

- [ ] `NETWORK 8` tab `Tx1..8`/`Rx1..8` `192.168.1.50:6980` `PCM_F32`/`VORBIS` `Q0.4` `Ch32` `BW` in `WHAControlPanelUI.cpp` one `ImGui` code for Master & Bridge, `INPUTS` `Type=NETWORK` `Rx1` linkage, `Per-stream` `codec` `Q` `Ch` `BW` wiring, `jitter 20/50ms` in `GENERAL` already done, `WHAControlPanelUI.cpp` `NETWORK 8` tab `ListClipper` `8` rows
